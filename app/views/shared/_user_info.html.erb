<a href="<%= user_path(current_user) %>" >
  <%= gravatar_for current_user, size: 52 %>
</a>
<h1>
  <%= current_user.name %>
</h1>
<h3>
  <%= link_to @mbtis.typecode, personality_path unless @mbtis.nil? %>

</h3>
<br/>
<br/>
<br/>
<span>
  <%= link_to "view my profile", current_user %>
</span>
<span>
  <%= pluralize(current_user.microposts.count, "woof") %>
</span> 